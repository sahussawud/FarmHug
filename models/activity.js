class activity {
    constructor(
      id,
      name,
      detail,
      type,
      farm_id,
      stall_id,
      cage_id,
      alertDate,
      is_routine,
      routineOptions,
      creater_id,

    ) {
      this.id = id;
      this.name = name;
      this.farm_id = farm_id;
      this.cage_id = cage_id;
      this.stall_id = stall_id;
      this.detail = detail;
      this.type = type;
      this.weight = weight;
      this.breed = breed;
    }
  }
  
  export default activity;
  